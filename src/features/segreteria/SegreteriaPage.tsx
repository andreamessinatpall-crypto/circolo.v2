import Segnaposto from '@/components/Segnaposto'

export default function SegreteriaPage() {
  return (
    <Segnaposto
      titolo="Segreteria"
      descrizione="Gestione soci, campi, regole, tornei e premi (solo amministratori) — in arrivo nella Fase 8."
    />
  )
}
